# Environment example for Property Dealer CRM
# Copy to `.env.local` and fill in real values for local development

MONGODB_URI=
SESSION_SECRET=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
NEXT_PUBLIC_APP_URL=
